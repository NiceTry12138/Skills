---
name: source-code-comprehension
description: 当用户请求阅读、学习、理解、分析、梳理函数、类、模块或项目源码时触发。系统化剖析代码的分层结构、核心数据结构、调用关系与时序流程，产出含 SVG 架构图的结构化阅读笔记到项目根目录的 ThroughtWork/ 下。关键词：读源码、分析项目、理解这个类、梳理框架、源码学习。
---

# 源码深度阅读 SKILL

## 核心原则（不可违反）

1. **基于代码**（最高优先级）：禁止通过名称、注释、文件路径推测任何对象的作用。具体要求：
   - **函数 / 方法**：必须打开源码逐行阅读函数体
   - **类 / 结构体**：必须阅读类定义（字段 + 关键方法 + 继承关系），不能只看类名
   - **属性 / 字段**：必须 grep 该字段名，查看至少 1 处写入和 1 处读取的上下文代码
   - **枚举值**：必须 grep 该枚举值，查看实际使用场景，不能仅按字面翻译
   - 注释、命名、文档字符串仅作为**辅助线索**，结论必须来自代码行为
2. **先筛后读**：先 grep 引用判断是否死代码，零引用的不深入
3. **可追溯**：每个关键结论都要能指向具体文件+行号
4. **诚实标注**：不确定的内容用 ⚠️ 显式声明，禁止伪装成确定结论
5. **拒绝偷懒**：
   - 用户标注"详细""着重""禁止概括"的内容，不允许一句话带过
   - 用户明确要求的内容，不允许用"⚠️ 不确定"跳过——必须实际读源码，
     真的读完仍不确定才能标注
   - 每个"算法/流程/计算"必须包含：原理 + 步骤 + 数值走查 + 边界

## 复杂用户需求路径

当用户提示词满足以下任一条件时，**必须先执行 Step 0 需求解析**：

- 提示词字数 > 200
- 包含"分别""对比"等并列要求词
- 包含具体数值场景（如示例数据）
- 给出了自检清单
- 明确要求多张 SVG/图片

不满足时可走标准 Step 1-6 流程。

## 工作流

### Step 0: 解析用户需求（首要步骤，不可跳过）

阅读用户提示词，提取并显式列出以下要素到工作目录的 `requirements.md`：

1. **必读范围**：用户明确指定的文件/类/函数（"必须阅读 X"）
2. **重点关注**：用户标注"着重""详细解释""重点"的内容
3. **对比维度**：用户要求"分别以 A/B/C 为例"的所有维度
4. **指定产出**：用户明确要求的 SVG / 流程图 / 示例图清单
5. **数值场景**：用户给出的具体数值例子（如"[1,2] → [1,3,4,5,6,7]"）
6. **格式约束**：用户给出的自检清单、禁止事项

产出 `requirements.md` 后，向用户确认："以下是我提取的要求清单，是否完整？"

**在 Step 6 产出笔记时，必须逐项对照 requirements.md，确保每条都有对应章节。**

### Step 1: 准备工作目录
按 `references/workdir-rules.md` 定位项目根并创建工作目录。
**关键**：工作目录必须在项目根正下方，禁止复用子模块下的同名目录。
开始写笔记前，向用户明确告知最终绝对路径。

### Step 2: 范围筛选
对范围内每个函数/类，用 grep/rg 搜索引用：
- 零引用 → 收入"未使用清单"，不深读
- 有引用 → 进入 Step 3

若预估核心符号 > 50 个，暂停并向用户确认收缩范围。

### Step 3: 精读代码

对每个符号执行**强制阅读动作**（不可跳过）：

| 符号类型 | 强制动作 |
|---------|---------|
| 函数 / 方法 | 打开文件，逐行阅读函数体，识别关键步骤与分支 |
| 类 / 结构体 | 阅读类定义（字段清单、关键方法签名、继承/接口关系） |
| 属性 / 字段 | grep 字段名 → 列出所有读写点 → 至少打开 1 处写入和 1 处读取上下文 |
| 枚举 | grep 各枚举值 → 查看实际判断/赋值场景 |

**跳过条件**：仅当该符号在 Step 2 被判定为零引用死代码时，才可不进入精读。

记录内容：位置、实际作用（基于代码逻辑）、关键步骤、调用方、依赖项。

**属性分级**：按 `references/property-doc-template.md` 的 A/B 级标准判定

A 级属性需要详细档案。**写档案前必须先 Read `references/property-doc-template.md`**，严格按其六要素结构填写。
B 级属性虽用简表，但同样需要先查看使用案例再写说明。

### Step 4: 深度解析复杂逻辑
当函数涉及算法、状态机、并发调度、数学/几何计算、多阶段流水线时，需深度解析。
**必须包含数值走查**（代入具体数字算一遍）。
**必须先 Read `references/algorithm-analysis-template.md`**，按其四要素结构产出。

### Step 5: 绘制架构图
在 `<工作目录>/Image/` 下生成 SVG。中文字体规范见 `references/svg-style-guide.md`。

**按需生成**（不强求数量）：
- `architecture.svg` —— 分层图，**必须含上游调用方和下游依赖**，不是目录树
- `call-graph.svg` —— 关键调用关系，箭头标方向
- `sequence-{流程名}.svg` —— 核心流程时序，用泳道区分角色（流程名用英文 kebab-case，如 `sequence-init.svg`、`sequence-data-sync.svg`）

若模块无对应内容（如纯数据类无时序流程），不强求生成对应图。

### Step 6: 产出笔记
**必须先 Read `references/note-template.md`**，按其完整结构产出 `through_{ts}.md`。

**核心要求**：
- 总览段明确写出**上游和下游**
- 分层至少含**外部接入层**和**底层依赖层**两个边界
- 若有主循环/Tick，调度伪代码必须画**分支判断**（不仅是顺序）
- 功能清单用**工程师视角**（数据结构+算法），不用产品视角（"支持 XX"）
- 未使用代码**独立成节**

## 触发边界（按规模自适应）

| 规模 | 产出策略 |
|------|---------|
| 单函数 / 小脚本 (<200 行) | 简化产出：总览 + 精读 + 可选 1 张图 |
| 单类 / 单模块 | 完整产出 |
| 整个项目 | 先产出总览 + 分层 + 模块清单，再向用户确认深入哪些模块 |

## 完成自检

### 用户需求落实
- [ ] requirements.md 中的每一项要求在最终笔记中都有对应章节，无遗漏
- [ ] 用户明确要求的对比维度（如四种属性对比）已逐项展开，无合并
- [ ] 用户给出的数值场景已在笔记中走查
- [ ] 用户明确要求的 SVG 全部生成，文件实际存在
- [ ] 用户给出的 SVG 自检清单已在回复中逐项确认

### 阅读约束（基于代码）
- [ ] 所有函数描述基于读过的函数体...
- [ ] 所有类描述基于读过的类定义...
- [ ] 所有属性（含 B 级）描述基于查看过的使用案例...
- [ ] 所有枚举值描述基于查看过的实际使用场景...

### 产出结构
- [ ] A 级属性档案含具体读写位置（文件:行号）
- [ ] 分层结构含外部接入层和底层依赖层...
- [ ] 复杂算法有数值走查
- [ ] SVG 中文显示正常
- [ ] 工程师视角...
- [ ] 未使用代码独立成节...

### 其他
- [ ] 不确定内容用 ⚠️ 标注
- [ ] 笔记和图片都在 `<项目根>/ThroughtWork/throught_{ts}/` 下